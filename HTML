<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>ESP32 Rover Control</title>
<style>
body{font-family:Arial;text-align:center;}
button{width:80px;height:50px;margin:3px;font-size:16px;}
button.active{background-color:green;color:white;}
#camFeed{border:2px solid #000;}
.field{margin:10px;padding:5px;border:1px solid #888;}
</style>
<!-- TensorFlow.js and COCO-SSD local offline -->
<script src="tf.min.js"></script>
<script src="coco-ssd.min.js"></script>
</head>
<body>

<h2>ESP32 Rover & Turret</h2>

<div class="field">
  <b>Camera Feed:</b><br>
  <video id="camFeed" width="320" height="240" autoplay muted></video>
</div>

<div class="field">
  <b>Rover Controls:</b><br>
  <button onmousedown="fetch('/fwd')" onmouseup="fetch('/stop')">Forward</button>
  <button onmousedown="fetch('/left')" onmouseup="fetch('/stop')">Left</button>
  <button onmousedown="fetch('/back')" onmouseup="fetch('/stop')">Back</button>
  <button onmousedown="fetch('/right')" onmouseup="fetch('/stop')">Right</button>
  <br>
  <button id="autoDriveBtn" onclick="toggleAuto('auto')">Auto Drive</button>
</div>

<div class="field">
  <b>Turret Controls:</b><br>
  <button onclick="fetch('/yawLeft')">Yaw Left</button>
  <button onclick="fetch('/yawRight')">Yaw Right</button>
  <button onclick="fetch('/yawStop')">Yaw Stop</button><br>
  <button onclick="fetch('/pitchUp')">Pitch Up</button>
  <button onclick="fetch('/pitchDown')">Pitch Down</button>
  <button onclick="fetch('/pitchStop')">Pitch Stop</button><br>
  <button id="autoCamBtn" onclick="toggleAuto('autoCam')">Auto Scan</button>
  <button id="sweepBtn" onclick="toggleAuto('sweep')">Sweep 360°</button>
</div>

<div class="field">
  <b>Battery / Sensor Info:</b><br>
  <div id="battery"></div>
  <div id="obstacle"></div>
  <div id="gps"></div>
</div>

<script>
// ---------------- Live Rover Info ----------------
function updateStatus(){
  fetch('/status').then(r=>r.json()).then(data=>{
    document.getElementById('battery').innerText="Battery: "+data.battery+" V ("+data.percent+"%)";
    document.getElementById('obstacle').innerText="Obstacle: "+data.distance+" cm";
    document.getElementById('gps').innerText="GPS: "+data.gps;
  });
}
setInterval(updateStatus,500);

// ---------------- Auto button toggle ----------------
function toggleAuto(type){
  fetch('/'+type).then(()=>updateBtn(type));
}
function updateBtn(type){
  fetch('/'+type+'Status').then(r=>r.text()).then(t=>{
    let btn = document.getElementById(type=='auto'?'autoDriveBtn':type=='autoCam'?'autoCamBtn':'sweepBtn');
    if(t=='ON'){ btn.classList.add('active'); }
    else{ btn.classList.remove('active'); }
  });
}
setInterval(()=>{updateBtn('auto');updateBtn('autoCam');updateBtn('sweep');},500);

// ---------------- Camera Stream ----------------
const video = document.getElementById('camFeed');
video.src = 'http://192.168.4.2:80/stream'; // Replace with your ESP-CAM IP

// ---------------- TensorFlow.js + COCO-SSD ----------------
let model;
cocoSsd.load().then(m=>{ model=m; console.log("Model loaded"); detect(); });

async function detect(){
  if(model){
    const predictions = await model.detect(video);
    predictions.forEach(p=>{
      if(p.class==='person'){
        // enemy logic: highlight red if black clothes, green otherwise
        console.log('Person detected:',p);
      }
    });
  }
  requestAnimationFrame(detect);
}
</script>
</body>
</html>
